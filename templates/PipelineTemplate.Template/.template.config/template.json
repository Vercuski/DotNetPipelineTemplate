{
  "$schema": "http://json.schemastore.org/template",
  "author": "Scott Vercuski",
  "classifications": ["Common", "Library"],
  "identity": "DotNetPipelineTemplate.PipesAndFilters",
  "name": "DotNet Pipeline Template (Pipes and Filters)",
  "_comment_shortName": "Finalized per ADR-0010 — checked for collisions against nuget.org before confirming. 'pipefilter' is the real short name, not a placeholder.",
  "shortName": "pipefilter",
  "sourceName": "PipelineApp",
  "preferNameDirectory": true,
  "symbols": {
    "IncludeSampleFilter": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "true",
      "description": "Include a trivial pass-through sample filter and a minimal pipeline in Program.cs to demonstrate usage. A consumer can opt out with --IncludeSampleFilter false."
    }
  },
  "sources": [
    {
      "modifiers": [
        {
          "condition": "(!IncludeSampleFilter)",
          "exclude": ["Filters/**"]
        }
      ]
    }
  ]
}
